#!/usr/bin/env python3
# ConstantStable picker stub.
#
# ConstantStable verticals are always 5-wide, so there is nothing to pick.
# This script exists only to keep the workflow structure compatible.

import sys

def main():
    print("ConstantStable picker: width fixed at 5; no dynamic selection.")
    return 0

if __name__ == "__main__":
    sys.exit(main())
